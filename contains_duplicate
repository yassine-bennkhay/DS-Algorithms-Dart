bool containDuplicate(List<int> array){
  List<int> helperArray=[];
  for(int i=0;i<array.length;i++){
    if(helperArray.contains(array[i])){
      return true;
    }
    else {
     helperArray.add(array[i]);
    }

  }
return false;
}
void main(){
print(containDuplicate([1,2,3,1]))
;}
